plugins {
    id 'application'
}

java {
    ext.version = 17
    sourceCompatibility = version
    targetCompatibility = version

    toolchain {
        languageVersion = JavaLanguageVersion.of(version)
    }
}

application {
    mainClass = 'io.github.jgacodili.molecules.client.main.Main'
}
